<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".menu">

    <LinearLayout
        android:id="@+id/layout1"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:layout_marginTop="40pt"
        android:layout_marginLeft="60dp">

        <!-- ImageButton en la posición (0, 0) -->
        <ImageButton
            android:id="@+id/imageButton1"
            android:layout_width="140dp"
            android:layout_height="140dp"
            android:src="@drawable/orange"
            android:scaleType="fitCenter" />

        <!-- ImageButton en la posición (0, 1) -->
        <ImageButton
            android:id="@+id/imageButton2"
            android:layout_width="140dp"
            android:layout_height="140dp"
            android:src="@drawable/pera"
            android:scaleType="fitCenter" />
    </LinearLayout>
    <LinearLayout
        android:id="@+id/layout2"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:layout_marginTop="108pt"
        android:layout_marginLeft="60dp">
        <!-- ImageButton en la posición (0, 2) -->
        <ImageButton
            android:id="@+id/imageButton3"
            android:layout_width="140dp"
            android:layout_height="140dp"
            android:src="@drawable/sandia"
            android:scaleType="fitCenter" />

        <!-- ImageButton en la posición (1, 0) -->
        <ImageButton
            android:id="@+id/imageButton4"
            android:layout_width="140dp"
            android:layout_height="140dp"
            android:src="@drawable/uvas"
            android:scaleType="fitCenter" />
    </LinearLayout>

</FrameLayout>